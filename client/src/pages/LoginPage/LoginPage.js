import React from 'react';
import { Background, style } from './LoginPageStyle';
export const LoginPage = () => {
  return (
    <Container>
      <Background></Background>
    </Container>
  );
};

const { Container } = style;
